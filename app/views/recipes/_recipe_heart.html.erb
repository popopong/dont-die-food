
  <% if @user_saved_recipe.length > 0 %>
    <%= link_to toggle_path(toggle_action: "destroy", recipe_id: recipe.id, saved_recipe_id: @user_saved_recipe.first.id)do%>
      <i class="saved fas fa-heart"></i>
    <% end %>
  <% else %>
    <%= link_to toggle_path(toggle_action: "create", recipe_id: recipe.id) do%>
      <i class="saved far fa-heart "></i>
    <% end %>
  <% end %>

